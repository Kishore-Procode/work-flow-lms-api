# Server Configuration
NODE_ENV=development
PORT=3000
API_VERSION=v1

# API Base URL (for Swagger documentation and external references)
# Leave empty to auto-generate from PORT (http://localhost:PORT)
# For production, set to your full API URL
# Example: https://api.yourdomain.com
API_BASE_URL=

# Database Configuration
DB_HOST=147.93.102.230
DB_PORT=5432
DB_NAME=student_act_lms
DB_USER=postgres
DB_PASSWORD=student_act@123
DB_SSL=false
DB_MAX_CONNECTIONS=100

# JWT Configuration
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production_student_act_lms_2024
JWT_EXPIRES_IN=7d
JWT_REFRESH_SECRET=your_refresh_token_secret_change_this_in_production_student_act_lms_2024
JWT_REFRESH_EXPIRES_IN=30d

# Email Configuration (for invitations and notifications)
SMTP_HOST=smtppro.zoho.in
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=info@cgfoundation.in
SMTP_PASSWORD=x3JWggqRpDQb
FROM_EMAIL=info@cgfoundation.in
FROM_NAME=Student-ACT LMS

# File Upload Configuration
# Base upload directory (relative to app root or absolute path)
UPLOAD_PATH=uploads
# For production deployment, use absolute path like:
# UPLOAD_PATH=/var/www/osot/uploads
# or relative to dist folder:
# UPLOAD_PATH=../uploads

MAX_FILE_SIZE=5242880
ALLOWED_FILE_TYPES=jpg,jpeg,png,pdf

# Static file serving configuration
# Base URL for serving uploaded files
STATIC_FILES_BASE_URL=/uploads
# For production with reverse proxy, might be:
# STATIC_FILES_BASE_URL=https://yourdomain.com/uploads

# Workflow Management System Files Path (for syllabus PDFs)
# Leave empty to auto-detect from /var/www/workflowmgmt/files
# The system will automatically find the correct drive letter on Windows (C:, D:, E:, etc.)
# On Linux, it will look for /var/www/workflowmgmt/files
# The syllabus PDFs are stored in this directory and served via /uploads route
WORKFLOW_FILES_PATH=
# To manually specify a path, use:
# WORKFLOW_FILES_PATH=D:\var\www\workflowmgmt\files (Windows)
# WORKFLOW_FILES_PATH=/var/www/workflowmgmt/files (Linux)

# Security Configuration
BCRYPT_ROUNDS=12
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Frontend URL Configuration (for CORS and email links)
# Primary frontend URL (backwards compatibility)
FRONTEND_URL=http://localhost:5173

# Multiple frontend URLs (comma-separated for CORS)
# Add all your frontend applications here
FRONTEND_URLS=http://localhost:5173,http://localhost:5174,http://localhost:3000,http://localhost:3001,http://127.0.0.1:5173

# Environment-specific frontend URLs
FRONTEND_DEV_URL=http://localhost:5173
FRONTEND_STAGING_URL=https://lms.student-act.org
FRONTEND_PROD_URL=https://lms.student-act.org

# Logging Configuration
LOG_LEVEL=info
LOG_FILE=logs/app.log

# API Documentation
SWAGGER_ENABLED=true

# Invitation Configuration
INVITATION_EXPIRES_DAYS=7
REGISTRATION_REQUEST_EXPIRES_DAYS=30

# Learning Progress Configuration
PROGRESS_REMINDER_DAYS=180
MEDIA_REQUIRED_TRACKING=true

# Nodemailer
EMAIL_USER=info@cgfoundation.in
EMAIL_PASS=x3JWggqRpDQb